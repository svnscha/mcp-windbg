name: Filter variant - non-callable hook fails fast
description: A process_output that is not callable is rejected, so the server fails to start.
negative_launch: true
server:
  args: ["--filter-script", "{scenarios_dir}/filters/not_callable.py"]
